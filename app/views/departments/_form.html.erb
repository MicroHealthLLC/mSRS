<%= form_with(model: department, local: true) do |form| %>
  <% if department.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(department.errors.count, "error") %> prohibited this department from being saved:</h2>

      <ul>
      <% department.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :plan_id %>
    <%= form.number_field :plan_id %>
  </div>

  <div class="field">
    <%= form.label :is_locked %>
    <%= form.check_box :is_locked %>
  </div>

  <div class="field">
    <%= form.label :strategic_description %>
    <%= form.text_area :strategic_description %>
  </div>

  <div class="field">
    <%= form.label :customer_description %>
    <%= form.text_area :customer_description %>
  </div>

  <div class="field">
    <%= form.label :internal_description %>
    <%= form.text_area :internal_description %>
  </div>

  <div class="field">
    <%= form.label :learning_description %>
    <%= form.text_area :learning_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
